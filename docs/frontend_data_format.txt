IN PROGRESS

argument format:
	obj = [day, day, ...]
		day = [route, route, ...]
		route = [size:int, title:str, [places]]
		places = [name:str, 



internal format:

	all[day] = {
		.route = [r, r, ...]		//base
		.weather
	}

	r = {
		.relativeSize				//base
		.title						
		.places = [pl, pl, ...]
	}

	pl = {
		.name
		.expectedDuration
		.expectedTravel
		.image
	}